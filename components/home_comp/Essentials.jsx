import SectionTitle from "../common/SectionTitle";

const Essentials = () => {
    return (
        <div>
                  <SectionTitle title={'Daily essentials. Essential daily.'} shortDescription={'Tap your way from aisle to aisle and order what you need, when you need it.'}/>

        </div>
    );
};

export default Essentials;